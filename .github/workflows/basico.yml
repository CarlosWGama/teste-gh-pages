name: Exemplo Básico
on: push
jobs:
  #Processo de teste
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Realiza os testes'

  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Manda o projeto para produção'